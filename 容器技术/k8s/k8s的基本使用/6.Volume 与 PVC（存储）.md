# Volume 与 PV/PVC（存储）

容器的总是临时的，当容器被重启或删除时，容器的数据便会消失。和Docker类似，k8s也提供了Volume（数据卷）和PVC（Persistent Volume Claim，持久卷声明）来实现数据的持久化。

## Volume（卷）

Volume本质上是一个目录，存在于宿主机上，并且被挂载到容器中。

### Volume类型


## Volume的使用

## 引入 PV / PVC

## 网络存储

## 动态卷供应

