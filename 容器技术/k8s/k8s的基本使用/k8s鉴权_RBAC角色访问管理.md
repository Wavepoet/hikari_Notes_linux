# RBAC（Role-Based Access Control，基于角色的访问控制）

RBAC是一种主流的权限管理模型，其权限分配方式为：将权限分配给``角色``,将角色绑定给主体。一个主体可以有多种角色。

>RBAC不仅仅用在k8s上，他是一种权限设计模型或者说模板，所有关于权限管理的设计时，都可以参考这个模板，做类似这个结构的权限管理。

- **主体**


- **角色**

## RBAC API

# RBAC的配置
