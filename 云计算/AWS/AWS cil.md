# AWS CLI

AWS CIL 是 AWS 官方提供的命令行工具，可以用来管理 AWS 资源。

## 安装

- debian/ubuntu:

```bash
sudo apt-get update
sudo apt-get install -y awscli

aws --version
```

- CentOS/RHEL/Fedora:

```bash
yum install -y awscli

aws --version
```

## 安全凭证


